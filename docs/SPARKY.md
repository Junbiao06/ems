# EMS 难点与亮点记录

这份文档用于积累可写进简历、面试中可展开说明的工程亮点。只在真正完成并验证后追加，不把计划中的功能写成既成事实。

###### 1. 将冲突规格整理成可执行的工程基线

完整梳理产品、API、安全、数据和测试主规格，并以用户最新决定覆盖旧方案：API 从 `/api/v1` 收敛为 `/api`，认证从 Cookie/Session 调整为 localStorage JWT，同时明确由此带来的 XSS、撤销和退出语义权衡。把未确认的离职、自动签退、共享 Contract 和 JWT 生命周期单独列为决策点，防止在数据库和核心流程中提前写死错误假设。

